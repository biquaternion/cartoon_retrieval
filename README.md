This is a simple demo of video retrieval


Usage:
1. put data into data folder
2. run the following
```
cd src
python run_clip_faiss.py
```
it will read frames, build faiss index and ask you for search query